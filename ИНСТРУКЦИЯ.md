# CopySnap - Инструкция по запуску

## Что это?

CopySnap - это готовый SaaS-сервис для AI-генерации контента. Пользователи платят подписку за доступ к инструментам копирайтинга на базе GPT-4o.

### Модель заработка:
- **Free**: $0 (5 генераций/день) — привлечение пользователей
- **Pro**: $12/мес (100 генераций/день)
- **Business**: $29/мес (безлимит)

**Цель**: 50 Pro + 15 Business = ~$1,035/мес

---

## Что нужно сделать (один раз)

### Шаг 1: Получить API-ключи

1. **OpenAI API key** — зайди на https://platform.openai.com/api-keys, создай ключ
   - Стоимость: ~$0.15 за 1 миллион токенов (очень дёшево)

2. **Stripe** — зайди на https://dashboard.stripe.com/register, создай аккаунт
   - Создай два продукта:
     - "Pro Plan" — $12/мес (recurring)
     - "Business Plan" — $29/мес (recurring)
   - Скопируй Price ID каждого продукта
   - В разделе Developers > API Keys скопируй Secret Key и Publishable Key

### Шаг 2: Запустить setup-скрипт

```bash
# Убедись что Node.js 18+ установлен
node -v

# Запусти скрипт
./setup.sh
```

Скрипт попросит ввести ключи и всё настроит автоматически.

### Шаг 3: Запустить приложение

```bash
npm run dev
```

Открой http://localhost:3000 — готово!

---

## Как задеплоить (чтобы другие могли пользоваться)

### Вариант 1: Railway (самый простой, ~$5/мес)

1. Зайди на https://railway.app
2. Создай проект из GitHub-репозитория
3. Добавь переменные окружения из файла `.env`
4. Railway сам задеплоит

### Вариант 2: VPS (DigitalOcean, Hetzner, ~$4-6/мес)

```bash
# На сервере:
git clone <твой-репозиторий>
cd copysnap
./setup.sh
npm run build
npm run start
```

Используй nginx как reverse proxy для порта 3000.

### Вариант 3: Vercel (бесплатно, но нужна внешняя БД)

1. Запушь в GitHub
2. Подключи на vercel.com
3. Замени SQLite на Vercel Postgres (нужна небольшая доработка)

---

## Как привлечь клиентов

1. **Product Hunt** — запустись там, это бесплатно и даёт первых пользователей
2. **Reddit** — посты в r/Entrepreneur, r/SaaS, r/marketing
3. **Twitter/X** — покажи генерации, люди любят AI-контент
4. **SEO** — напиши блог-посты о копирайтинге (используя свой же инструмент!)
5. **Facebook/LinkedIn группы** — маркетологи и предприниматели — твоя ЦА
6. **AppSumo** — можно продать lifetime-доступ для начального притока
7. **Бесплатный план** — самый мощный канал привлечения. Люди пробуют, им нравится, апгрейдятся

---

## Стоимость содержания

| Статья расходов | Стоимость |
|----------------|-----------|
| Хостинг (Railway/VPS) | $5/мес |
| OpenAI API | ~$5-20/мес (зависит от кол-ва пользователей) |
| Домен | ~$10/год |
| **Итого** | ~$15-30/мес |

При доходе $1,000/мес чистая прибыль: **~$970/мес**

---

## Поддержка

Весь код структурирован и прокомментирован. Основные файлы:

- `src/app/page.tsx` — лендинг
- `src/app/dashboard/page.tsx` — генератор контента
- `src/app/api/generate/route.ts` — AI-генерация
- `src/app/api/stripe/` — платежи
- `src/lib/` — утилиты (БД, авторизация, OpenAI, Stripe)
- `prisma/schema.prisma` — схема базы данных
